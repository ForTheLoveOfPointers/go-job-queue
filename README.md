# go-job-queue
This is a job queue API made purely with Go (using Chi).
It uses concurrency, channels and context in an idiomatic way with no
additional dependencies.

It merely shows I have mastered the very basics to use in complex architectures and
code bases.